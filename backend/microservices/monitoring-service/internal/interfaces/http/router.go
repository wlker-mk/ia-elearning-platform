package http

import "github.com/gin-gonic/gin"

// SetupRoutes configure toutes les routes de l'application
func SetupRoutes(r *gin.Engine) {
	// Les routes sont configurées dans server.go
	// Ce fichier peut être utilisé pour des configurations additionnelles
}
